# Generated by the protocol buffer compiler.  DO NOT EDIT!
# sources: envoy/extensions/filters/http/basic_auth/v3/basic_auth.proto
# plugin: python-betterproto
# This file has been @generated

from dataclasses import dataclass

import betterproto

from ......config.core import v3 as _____config_core_v3__


@dataclass(eq=False, repr=False)
class BasicAuth(betterproto.Message):
    """
    Basic HTTP authentication. Example: .. code-block:: yaml   users:
    inline_string: |-       user1:{SHA}hashed_user1_password
    user2:{SHA}hashed_user2_password
    """

    users: "_____config_core_v3__.DataSource" = betterproto.message_field(1)
    """
    Username-password pairs used to verify user credentials in the
    "Authorization" header. The value needs to be the htpasswd format.
    Reference to https://httpd.apache.org/docs/2.4/programs/htpasswd.html
    """

    forward_username_header: str = betterproto.string_field(2)
    """
    This field specifies the header name to forward a successfully
    authenticated user to the backend. The header will be added to the request
    with the username as the value. If it is not specified, the username will
    not be forwarded.
    """

    authentication_header: str = betterproto.string_field(3)
    """
    This field specifies the request header to load the basic credential from.
    If it is not specified, the filter loads the credential from  the
    "Authorization" header.
    """


@dataclass(eq=False, repr=False)
class BasicAuthPerRoute(betterproto.Message):
    """
    Extra settings that may be added to per-route configuration for a virtual
    host or a cluster.
    """

    users: "_____config_core_v3__.DataSource" = betterproto.message_field(1)
    """Username-password pairs for this route."""
