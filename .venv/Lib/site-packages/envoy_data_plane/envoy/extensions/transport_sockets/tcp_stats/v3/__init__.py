# Generated by the protocol buffer compiler.  DO NOT EDIT!
# sources: envoy/extensions/transport_sockets/tcp_stats/v3/tcp_stats.proto
# plugin: python-betterproto
# This file has been @generated

from dataclasses import dataclass
from datetime import timedelta

import betterproto

from .....config.core import v3 as ____config_core_v3__


@dataclass(eq=False, repr=False)
class Config(betterproto.Message):
    """
    Configuration for the TCP Stats transport socket wrapper, which wraps
    another transport socket for all communication, but emits stats about the
    underlying TCP connection. The stats are documented :ref:`here
    <config_listener_stats_tcp>` for listeners and :ref:`here
    <config_cluster_manager_cluster_stats_tcp>` for clusters. This transport
    socket is currently only supported on Linux.
    """

    transport_socket: "____config_core_v3__.TransportSocket" = (
        betterproto.message_field(1)
    )
    """The underlying transport socket being wrapped."""

    update_period: timedelta = betterproto.message_field(2)
    """
    Period to update stats while the connection is open. If unset, updates only
    happen when the connection is closed. Stats are always updated one final
    time when the connection is closed.
    """
