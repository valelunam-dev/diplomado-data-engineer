# Generated by the protocol buffer compiler.  DO NOT EDIT!
# sources: envoy/extensions/stat_sinks/open_telemetry/v3/open_telemetry.proto
# plugin: python-betterproto
# This file has been @generated

from dataclasses import dataclass
from typing import Optional

import betterproto

from .....config.core import v3 as ____config_core_v3__


@dataclass(eq=False, repr=False)
class SinkConfig(betterproto.Message):
    """[#next-free-field: 7]"""

    grpc_service: "____config_core_v3__.GrpcService" = betterproto.message_field(
        1, group="protocol_specifier"
    )
    """The upstream gRPC cluster that implements the OTLP/gRPC collector."""

    report_counters_as_deltas: bool = betterproto.bool_field(2)
    """
    If set to true, counters will be emitted as deltas, and the OTLP message
    will have ``AGGREGATION_TEMPORALITY_DELTA`` set as AggregationTemporality.
    """

    report_histograms_as_deltas: bool = betterproto.bool_field(3)
    """
    If set to true, histograms will be emitted as deltas, and the OTLP message
    will have ``AGGREGATION_TEMPORALITY_DELTA`` set as AggregationTemporality.
    """

    emit_tags_as_attributes: Optional[bool] = betterproto.message_field(
        4, wraps=betterproto.TYPE_BOOL
    )
    """
    If set to true, metrics will have their tags emitted as OTLP attributes,
    which may contain values used by the tag extractor or additional tags added
    during stats creation. Otherwise, no attributes will be associated with the
    export message. Default value is true.
    """

    use_tag_extracted_name: Optional[bool] = betterproto.message_field(
        5, wraps=betterproto.TYPE_BOOL
    )
    """
    If set to true, metric names will be represented as the tag extracted name
    instead of the full metric name. Default value is true.
    """

    prefix: str = betterproto.string_field(6)
    """
    If set, emitted stats names will be prepended with a prefix, so full stat
    name will be <prefix>.<stats_name>. For example, if the stat name is
    "foo.bar" and prefix is "pre", the full stat name will be "pre.foo.bar". If
    this field is not set, there is no prefix added. According to the example,
    the full stat name will remain "foo.bar".
    """
