# Generated by the protocol buffer compiler.  DO NOT EDIT!
# sources: envoy/extensions/geoip_providers/common/v3/common.proto
# plugin: python-betterproto
# This file has been @generated

from dataclasses import dataclass

import betterproto


@dataclass(eq=False, repr=False)
class CommonGeoipProviderConfig(betterproto.Message):
    geo_headers_to_add: "CommonGeoipProviderConfigGeolocationHeadersToAdd" = (
        betterproto.message_field(1)
    )
    """Configuration for geolocation headers to add to request."""


@dataclass(eq=False, repr=False)
class CommonGeoipProviderConfigGeolocationHeadersToAdd(betterproto.Message):
    """
    The set of geolocation headers to add to request. If any of the configured
    headers is present in the incoming request, it will be overridden by the
    :ref:`Geoip filter <config_http_filters_geoip>`. [#next-free-field: 10]
    """

    country: str = betterproto.string_field(1)
    """
    If set, the header will be used to populate the country ISO code associated
    with the IP address.
    """

    city: str = betterproto.string_field(2)
    """
    If set, the header will be used to populate the city associated with the IP
    address.
    """

    region: str = betterproto.string_field(3)
    """
    If set, the header will be used to populate the region ISO code associated
    with the IP address. The least specific subdivision will be selected as
    region value.
    """

    asn: str = betterproto.string_field(4)
    """
    If set, the header will be used to populate the ASN associated with the IP
    address.
    """

    is_anon: str = betterproto.string_field(5)
    """
    If set, the IP address will be checked if it belongs to any type of
    anonymization network (e.g. VPN, public proxy etc) and header will be
    populated with the check result. Header value will be set to either "true"
    or "false" depending on the check result.
    """

    anon_vpn: str = betterproto.string_field(6)
    """
    If set, the IP address will be checked if it belongs to a VPN and header
    will be populated with the check result. Header value will be set to either
    "true" or "false" depending on the check result.
    """

    anon_hosting: str = betterproto.string_field(7)
    """
    If set, the IP address will be checked if it belongs to a hosting provider
    and header will be populated with the check result. Header value will be
    set to either "true" or "false" depending on the check result.
    """

    anon_tor: str = betterproto.string_field(8)
    """
    If set, the IP address will be checked if it belongs to a TOR exit node and
    header will be populated with the check result. Header value will be set to
    either "true" or "false" depending on the check result.
    """

    anon_proxy: str = betterproto.string_field(9)
    """
    If set, the IP address will be checked if it belongs to a public proxy and
    header will be populated with the check result. Header value will be set to
    either "true" or "false" depending on the check result.
    """
